

# The URL below is where the configuration files can be found.
#
# If the url is a file: resource, then it will read all the files in
# that directory, using the File.listFiles() method.
# If the url is a http: or ftp: resource, then it will request the
# index.lst file at that location, which must return a line separated
# list of files at that location containing the configurations.
#
# The directory is relative to ${dpml.home}/transit.
# URLs may also be used to centralize the definitions.
#

dpml.transit.authority=authority/

