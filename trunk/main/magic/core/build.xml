<?xml version="1.0" encoding="UTF-8" ?>
<!--
  Copyright 2004 Stephen J McConnell
  Copyright 2004 Niclas Hedhman

  Licensed  under the  Apache License,  Version 2.0  (the "License");
  you may not use  this file  except in  compliance with the License.
  You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed  under the  License is distributed on an "AS IS" BASIS,
  WITHOUT  WARRANTIES OR CONDITIONS  OF ANY KIND, either  express  or
  implied.

  See the License for the specific language governing permissions and
  limitations under the License.
-->

<project name="dpml-magic-core" default="install" basedir="."
    xmlns:transit="antlib:net.dpml.transit">

  <transit:import file="${dpml.prefs}/transit/templates/bootstrap.template"/>

  <target name="init" depends="bootstrap.init">

    <echo>Building Magic core using Transit ${dpml.transit.version}</echo>

    <property name="dpml-magic-core.uri"
        value="artifact:plugin:dpml/magic/dpml-magic-core#${project.version}"/>
    <filter token="PROJECT-PLUGIN-URN" value="plugin:dpml/magic/dpml-magic-core"/>
    <filter token="PROJECT-PLUGIN-URI" value="${dpml-magic-core.uri}"/>
    <filter token="PROJECT-PLUGIN-LINK-URI" value="link:plugin:dpml/magic/dpml-magic-core"/>

    <!-- installer filters -->
    <filter token="JUNIT-URI" value="artifact:jar:junit/junit#${junit.version}"/>
    <filter token="TRANSIT-MAIN-URI" 
       value="artifact:jar:dpml/transit/dpml-transit-main#${dpml.transit.version}"/>
    <filter token="TRANSIT-TOOLS-URI" 
       value="artifact:jar:dpml/transit/dpml-transit-tools#${dpml.transit.version}"/>
  </target>

  <target name="setup-path" depends="bootstrap.setup-path">
    <transit:get property="checkstyle.path" uri="${checkstyle.uri}"/>
    <path id="project.main.path">
      <pathelement location="${dpml.cache}/dpml/transit/jars/dpml-transit-main-${dpml.transit.version}.jar"/>
      <pathelement location="${dpml.cache}/dpml/transit/jars/dpml-transit-tools-${dpml.transit.version}.jar"/>
      <pathelement location="${checkstyle.path}"/>
      <pathelement location="${basedir}/target/classes"/>
    </path>
  </target>

  <target name="build" depends="bootstrap.build">
    <taskdef name="export" classname="net.dpml.magic.tasks.ExportTask">
      <classpath refid="project.main.path"/>
    </taskdef>
    <echo>plugin:dpml/magic/dpml-magic-core#${project.version}</echo>
    <export>
      <antlib resource="net/dpml/magic/plugin.xml" 
        urn="plugin:dpml/magic/dpml-magic-core#${project.version}"/>
    </export>
    <property name="link" location="${basedir}/target/deliverables/plugins/${project.name}.plugin.link"/>
    <echo file="${link}">artifact:plugin:dpml/magic/${project.name}#${project.version}</echo>
  </target>

  <target name="install" depends="bootstrap.install">
    <property name="formats" value="${dpml.prefs}/magic/formats"/>
    <mkdir dir="${formats}"/>
    <copy toDir="${formats}">
      <fileset dir="target/formats"/>
    </copy>
    <property name="xsls" value="${dpml.prefs}/magic/xsls"/>
    <mkdir dir="${xsls}"/>
    <copy toDir="${xsls}">
      <fileset dir="target/xsls"/>
    </copy>
    <property name="templates" value="${dpml.prefs}/magic/templates"/>
    <mkdir dir="${templates}"/>
    <copy toDir="${templates}">
      <fileset dir="target/templates"/>
    </copy>
  </target>


</project>
