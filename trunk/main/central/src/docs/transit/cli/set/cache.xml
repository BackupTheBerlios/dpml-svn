<?xml version="1.0" encoding="UTF-8"?>

<document>

  <properties>
    <author email="mcconnell@dpml.net">Stephen McConnell</author>
    <title>DPML Transit</title>
  </properties> 

  <body>

    <section name="Set Cache Command">

<pre><![CDATA[$ transit set cache 
  [-cache <path>
  [-system <path>]
  [-layout <id>]
]]></pre>

      <subsection name="Description">

        <p>
        Updates the cache configuration.
        </p>

      </subsection>

      <subsection name="Options">

        <table>
          <tr><td>cache</td><td>The cache directory path.  The path may
            contain references to system properties in the form 
            <tt>${key}</tt>.  Typically the cache path is declared 
            relative to the DPML Data root via the <tt>${dpml.data}</tt>
            property.</td></tr>
          <tr><td>system</td><td>Location of the local system resource 
            repository.  The path may contain references to system 
            properties in the form <tt>${key}</tt>.  Typically the 
            system path is declared relative to the DPML System root 
            via the <tt>${dpml.system}</tt> property.</td></tr>
          <tr><td>layout</td><td>The id of the layout strategy used 
             within the cache.</td></tr>
        </table>

      </subsection>

    </section>

  </body>

</document>

